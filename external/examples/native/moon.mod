name = "colmugx/posoco-example"

version = "0.1.0"

import {
  "colmugx/posoco@0.3.0",
  "moonbitlang/async@0.19.1",
}

readme = "README.mdt.md"

license = "Apache-2.0"

keywords = [ "posoco", "example", "demo", "repl" ]

description = "Posoco Example REPL Agent — real-world agent demo with OpenAI + shell tools + streaming"

options(
  preferred_target: "native",
)